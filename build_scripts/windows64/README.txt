setup_env.bat -sets up the build environment
run_qmake.bat -sets up nmake projects using qmake
run_nmake.bat -builds using nmake
run.bat -sets up environment, runs qmake, and builds the release version of caret
run_all.bat -setups environment, runs qmake, and build the release and debug versions of caret